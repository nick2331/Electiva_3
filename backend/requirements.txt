# Web framework
fastapi==0.115.5
uvicorn[standard]==0.32.1
python-multipart==0.0.12
pydantic==2.10.3
pydantic-settings==2.7.0

# Database
sqlalchemy==2.0.36
aiosqlite==0.20.0
asyncpg==0.30.0

# Deep learning — CPU-only para reducir tamaño en Render free tier (~800 MB vs 2 GB GPU)
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.5.1+cpu
torchvision==0.20.1+cpu
timm==1.0.12
Pillow==11.0.0

# Computer vision
opencv-python-headless==4.10.0.84

# Interpretabilidad (Grad-CAM) — opcional, no bloquea si falta
grad-cam==1.5.4

# PDF — WeasyPrint requiere librerías del sistema; se instala si están disponibles
weasyprint==62.3

# Utilities
numpy==1.26.4
aiofiles==24.1.0
